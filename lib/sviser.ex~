defmodule Sviser do
@moduledoc false

	def start do
		children = 
		[
			%{
				id: Superviser,
				start: {Superviser, :start, []}
			}
		]

		IO.puts("start Svis")
		Supervisor.start_link(children, strategy: :one_for_one)
	end
end
